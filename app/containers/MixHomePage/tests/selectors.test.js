// import { fromJS } from 'immutable';
// import { makeSelectMixHomePageDomain } from '../selectors';

// const selector = makeSelectMixHomePageDomain();

describe('makeSelectMixHomePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
