// import { fromJS } from 'immutable';
// import { makeSelectEnergieMixPageDomain } from '../selectors';

// const selector = makeSelectEnergieMixPageDomain();

describe('makeSelectEnergieMixPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
